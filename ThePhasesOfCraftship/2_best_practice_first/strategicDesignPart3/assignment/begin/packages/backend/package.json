{
  "name": "@dddforum/backend",
  "version": "1.0.0",
  "description": "A basic typescript app starter for 2023.",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "clean": "rimraf ./build",    
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --skip-seed",
    "db:seed": "npm run db:reset && prisma db seed",
    "db:reset": "rimraf ./dev.db && npm run migrate && npm run generate",
    "start:dev": "npm run migrate && npm run generate && npm run db:seed && npx nodemon",
    "start": "npm run build && node build/index.js",
    "lint": "eslint . --ext .ts",
    "prettier-format": "run-script-os",
    "prettier-format:win32": "prettier --config .prettierrc \"./src/**/*.ts\" --write",
    "prettier-format:darwin:linux": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "prettier-format:default": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "prettier-watch": "run-script-os",
    "prettier-watch:win32": "onchange \"src/**/*.ts\" -- prettier --write {{changed}}",
    "prettier-watch:darwin:linux": "onchange 'src/**/*.ts' -- prettier --write {{changed}}",
    "prettier-watch:default": "onchange 'src/**/*.ts' -- prettier --write {{changed}}",
    "test:e2e": "jest -c jest.config.e2e.ts",
    "test:dev": "jest --watchAll"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run test && npm run prettier-format && npm run lint"
    }
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.17",
    "@types/nodemailer": "^6.4.15",
    "@types/supertest": "^6.0.2",
    "dotenv": "^16.3.1",
    "dotenv-cli": "^7.3.0",
    "nodemon": "^2.0.19",
    "supertest": "^7.0.0"
  },
  "dependencies": {
    "@prisma/client": "^4.16.0",
    "@dddforum/shared": "file:../shared",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jest-cucumber": "^3.0.1",
    "pg": "^8.11.0",
    "prisma": "^4.16.0"
  }
}
